{
  "name": "starterfiles",
  "version": "1.0.0",
  "description": "",
  "main": "scripts.js",
  "scripts": {
    "watch" : "concurrently \"npm run server\" \"babel src -d dist --presets=es2015 -ws\"",
    "build" : "rm -rf dist && babel src -d dist --presets=es2015",
    "server" : "http-server",
    "babel" : "babel src -d dist --presets=es2015"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "concurrently": "^5.1.0",
    "http-server": "^0.12.1"
  }
}
